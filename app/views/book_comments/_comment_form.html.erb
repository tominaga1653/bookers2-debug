<%= form_with model: [book, book_comment], local: false do |f| %>
  <div class="form-group">
    <%= f.text_area :comment, rows: '2', class: 'form-control comment-textarea' %>
  </div>
  <div class="form-group">
    <%= f.submit "送信" %>
  </div>
<% end %>